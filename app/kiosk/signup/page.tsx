'use client';

export default function SignupPlaceholder() {
  return (
    <main style={{ padding: 20 }}>
      <h2>Inscripción de familia</h2>
      <p>Esta es una pantalla temporal para verificar el despliegue.</p>
      <p>Cuando el deploy quede en <b>Ready</b>, añadiremos el formulario real aquí.</p>
      <a href="/" style={{ display: 'inline-block', marginTop: 16 }}>← Volver al inicio</a>
    </main>
  );
}
